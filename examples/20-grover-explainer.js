import { explainQubi } from '../src/index.js';

export const config = { theme: 'qubibyte' };

// Narrated Grover search for 110, generated entirely from the program.
export default explainQubi('Grover(0b110)');
